# Activation du module rewrite
RewriteEngine On

# Redirection de la racine vers exacueil.php
RewriteRule ^$ /exacueil.php [L]

# Protection contre l'accès direct aux fichiers sensibles
<FilesMatch "\.(php|inc|sql)$">
    Require all denied
</FilesMatch>
